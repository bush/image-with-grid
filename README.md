# Adding a grid to the image

# spacing should be set at 5.0 and should remain consistent for future comparison
# viewpoint should be set at 'Dist: 48", Height: 50"' and should remain consistent for future comparison

```
python add_grid.py --spacing 5.0 --weight "219.4 lbs" --viewpoint 'Dist: 48", Height: 50"'
```
